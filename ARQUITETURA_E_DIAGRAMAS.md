# 🎨 Guia Visual e Diagramas - Chronos Pomodoro

## 📊 Fluxo de Dados da Aplicação

```
┌─────────────────────────────────────────────────────────┐
│                     App (Root)                          │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │      TextContextProvider (State Management)      │  │
│  │                                                  │  │
│  │  ┌────────────────────────────────────────────┐ │  │
│  │  │ useReducer(taskReducer, initialTaskState) │ │  │
│  │  │                                            │ │  │
│  │  │ dispatch(action)  ──► taskReducer         │ │  │
│  │  │                        └─► new state      │ │  │
│  │  └────────────────────────────────────────────┘ │  │
│  │                                                  │  │
│  │  ┌────────────────────────────────────────────┐ │  │
│  │  │ TimerWorkerSingleton (Background Timer)   │ │  │
│  │  │                                            │ │  │
│  │  │ postMessage(state) ─► Worker.js          │ │  │
│  │  │      ◄── onmessage(secondsLeft)          │ │  │
│  │  └────────────────────────────────────────────┘ │  │
│  │                                                  │  │
│  │  localStorage.setItem("state", JSON.stringify)   │  │
│  └──────────────────────────────────────────────────┘  │
│                      ▼                                  │
│  ┌──────────────────────────────────────────────────┐  │
│  │         ShowMessage (Toast Notifications)        │  │
│  └──────────────────────────────────────────────────┘  │
│                      ▼                                  │
│  ┌──────────────────────────────────────────────────┐  │
│  │              MainRouter (SPA Routes)             │  │
│  │  ├─ /           ► Home                          │  │
│  │  ├─ /history    ► History                       │  │
│  │  ├─ /settings   ► Settings                      │  │
│  │  ├─ /about      ► AboutPomodoro                 │  │
│  │  └─ *           ► NotFound (404)                │  │
│  └──────────────────────────────────────────────────┘  │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 🔄 Ciclo de Vida de uma Tarefa

```
                    ┌─────────────┐
                    │   Usuário   │
                    │ Cria Tarefa │
                    └──────┬──────┘
                           │
                           ▼
                 ┌──────────────────────┐
                 │  Validar Input       │
                 │  (nome não vazio)    │
                 └──────┬───────────────┘
                        │
                        ▼
              ┌─────────────────────────┐
              │ Criar TaskModel         │
              │ - id (timestamp)        │
              │ - name                  │
              │ - duration              │
              │ - startDate (now)       │
              │ - type (próximo ciclo)  │
              └──────┬──────────────────┘
                     │
                     ▼
              ┌─────────────────────────┐
              │ dispatch(START_TASK)    │
              │                         │
              │ taskReducer:            │
              │ - Atualiza activeTask   │
              │ - Incrementa ciclo      │
              │ - Converte para segundos│
              │ - Adiciona ao tasks[]   │
              └──────┬──────────────────┘
                     │
                     ▼
              ┌─────────────────────────┐
              │ localStorage.setItem()  │
              │ (estado persistido)     │
              └──────┬──────────────────┘
                     │
                     ▼
         ┌───────────────────────────────┐
         │ TimerWorker.postMessage()     │
         │ Inicia countdown em bg        │
         │                               │
         │ Tick a cada 1 segundo:        │
         │ postMessage(secondsLeft)      │
         └──────┬────────────────────────┘
                │
         ┌──────┴─────────────────────┐
         │                            │
      [RUNNING]                  [CONCLUÍDO?]
    (COUNT_DOWN                      │
     action)                   seconds <= 0
         │                            │
         ▼                            ▼
    ┌─────────┐              ┌──────────────┐
    │ UI      │              │ playBeep()   │
    │ atualiza│              │              │
    │ timer   │              │ dispatch     │
    │ MM:SS   │              │ COMPLETE_    │
    └─────────┘              │ TASK         │
                             └────┬─────────┘
                                  │
                                  ▼
                         ┌────────────────┐
                         │ taskReducer:   │
                         │ - activeTask=0 │
                         │ - completeDate │
                         │   = Date.now() │
                         │ - tasks[] atualizado
                         └────┬───────────┘
                              │
                              ▼
                         ┌────────────────┐
                         │ Task Concluída │
                         └────────────────┘
```

---

## 🧬 Estrutura de Estados e Transições

```
TaskState Inicial:
{
  tasks: [],
  secondsRemaining: 0,
  formattedSecondsRemaining: "00:00",
  activeTask: null,
  currentCycle: 0,
  config: { workTime: 1, shortBreakTime: 1, longBreakTime: 1 }
}


                    ┌──────────────┐
                    │ START_TASK   │
                    │ (novo task)  │
                    └────┬─────────┘
                         │
        ┌────────────────────────────────┐
        │          TaskState             │
        │  ─────────────────────────────  │
        │  activeTask: TaskModel          │
        │  currentCycle: ++               │
        │  secondsRemaining: duration*60  │
        │  tasks: [..., newTask]          │
        │  formattedSecondsRemaining: MM:SS
        └───┬────────────────┬────────────┘
            │                │
    ┌───────┴──┐      ┌──────┴─────────┐
    │ COUNT_DOWN│      │ INTERRUPT_TASK/ │
    │ (tick)   │      │ COMPLETE_TASK   │
    │          │      │                 │
    └────┬─────┘      └────┬────────────┘
         │                 │
    secondsRemaining--   activeTask: null
    formatSeconds()      secondsRemaining: 0
                         completeDate/interruptDate set
                         └─► RESET_TASK
                              ▼
                         State Inicial
```

---

## 🎯 Lógica de Determinação de Ciclos

```
CurrentCycle │ getNextCycle() │ getNextCycleType() │ Duração
─────────────┼────────────────┼───────────────────┼──────────────
      0      │       1        │    "workTime"     │ workTime
      1      │       2        │  "shortBreakTime" │ 1 min
      2      │       3        │    "workTime"     │ workTime
      3      │       4        │  "shortBreakTime" │ 1 min
      4      │       5        │    "workTime"     │ workTime
      5      │       6        │  "shortBreakTime" │ 1 min
      6      │       7        │    "workTime"     │ workTime
      7      │       8        │  "longBreakTime"  │ 1 min
      8      │       1        │    "workTime"     │ workTime (reset)


Algoritmo:
─────────

function getNextCycleType(cycle: number) {
  if (cycle % 8 === 0) return "longBreakTime"      // 8, 16, 24...
  if (cycle % 2 === 0) return "shortBreakTime"    // 2, 4, 6...
  return "workTime"                               // 1, 3, 5, 7...
}

Visual dos Ciclos (Cycles Component):
────────────────────────────────────

Ciclos: 🔵 🟢 🔵 🟢 🔵 🟢 🔵 🟣
        │  │  │  │  │  │  │  │
        1  2  3  4  5  6  7  8
        
Legenda:
  🔵 = workTime (foco)
  🟢 = shortBreakTime (pausa curta)
  🟣 = longBreakTime (pausa longa)
```

---

## 📁 Arquitetura em Camadas

```
┌────────────────────────────────────────┐
│     PRESENTATION LAYER                 │
│  ┌──────────────────────────────────┐ │
│  │ Pages (Home, Settings, etc)      │ │
│  │ Components (UI elements)         │ │
│  │ Templates (Layout)               │ │
│  └──────────────────────────────────┘ │
└────────────┬─────────────────────────┘
             │ useTaskContext()
             ▼
┌────────────────────────────────────────┐
│     STATE MANAGEMENT LAYER             │
│  ┌──────────────────────────────────┐ │
│  │ TaskContext (React Context)      │ │
│  │ TaskContextProvider              │ │
│  │ useTaskContext Hook              │ │
│  └──────────────────────────────────┘ │
│  ┌──────────────────────────────────┐ │
│  │ taskReducer (State Logic)        │ │
│  │ taskAction (Action Types)        │ │
│  │ initialTaskState                 │ │
│  └──────────────────────────────────┘ │
└────────────┬─────────────────────────┘
             │ postMessage/onmessage
             ▼
┌────────────────────────────────────────┐
│     WORKER LAYER (Background)          │
│  ┌──────────────────────────────────┐ │
│  │ TimerWorkerSingleton             │ │
│  │ timeWorker.js (Countdown logic)  │ │
│  └──────────────────────────────────┘ │
└────────────┬─────────────────────────┘
             │ localStorage
             ▼
┌────────────────────────────────────────┐
│     PERSISTENCE LAYER                  │
│  ┌──────────────────────────────────┐ │
│  │ localStorage (Browser Storage)   │ │
│  │ Key: "state"                     │ │
│  └──────────────────────────────────┘ │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│     UTILITIES LAYER                    │
│  ┌──────────────────────────────────┐ │
│  │ formatters (formatDate, etc)     │ │
│  │ getters (getNextCycle, etc)      │ │
│  │ adapters (showMessage)           │ │
│  └──────────────────────────────────┘ │
└────────────────────────────────────────┘
```

---

## 🔀 Fluxo de Actions no Reducer

```
dispatch({type, [payload]})
           │
           ▼
    ┌──────────────────┐
    │  taskReducer()   │
    └────┬─────────────┘
         │
    ┌────┴───────────────────────────────────┐
    │                                        │
    ▼                                        ▼
START_TASK                            INTERRUPT_TASK
    │                                        │
    ├─ Incrementar cycle                    ├─ Limpar activeTask
    ├─ Calcular duration*60                 ├─ Zerar secondsRemaining
    ├─ Adicionar a tasks[]                  ├─ Marcar interruptDate
    ├─ Setar activeTask                     └─ Salvar em JSON
    ├─ Formatar MM:SS
    └─ Salvar em JSON
    
    │                                        │
    ▼                                        ▼
COMPLETE_TASK                         COUNT_DOWN
    │                                        │
    ├─ Limpar activeTask                    ├─ Decrementar secondsRemaining
    ├─ Zerar secondsRemaining               ├─ Formatar para MM:SS
    ├─ Marcar completeDate                  └─ Salvar em JSON
    ├─ Novas tasks marcado
    └─ Salvar em JSON                   RESET_TASK
                                            │
                                            ├─ Voltar ao initialTaskState
                                            └─ Limpar tudo
```

---

## 🖼 Wireframe da Interface

```
┌─────────────────────────────────────────────────────────┐
│                     HEADER / MENU                       │
│  Chronos Pomodoro | Home | History | Settings | About   │
├─────────────────────────────────────────────────────────┤
│                                                         │
│                     ┌───────────────┐                  │
│                     │   MM:SS       │  ◄─ CountDown   │
│                     │  (Timer)      │                  │
│                     └───────────────┘                  │
│                                                         │
│              ┌───────────────────────────┐             │
│              │  Criar Nova Tarefa        │             │
│              │  ┌─────────────────────┐  │             │
│              │  │ [Input Task Name ] │  │  ◄─ MainForm
│              │  └─────────────────────┘  │             │
│              │  Ciclos: 🔵 🟢 🔵          │             │
│              │  Dica: "Mantenha foco"    │             │
│              │  ┌──────────┬──────────┐  │             │
│              │  │ ▶ Iniciar│ ⏹ Parar │  │             │
│              │  └──────────┴──────────┘  │             │
│              └───────────────────────────┘             │
│                                                         │
├─────────────────────────────────────────────────────────┤
│                       FOOTER                           │
│           © 2026 Chronos Pomodoro                      │
└─────────────────────────────────────────────────────────┘
```

---

## 📱 Tela de Histórico

```
┌──────────────────────────────────────────────────────────┐
│                     HISTÓRICO DE TAREFAS                │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  Nome          │ Data      │ Tipo      │ Status   │ Del │
│  ────────────────────────────────────────────────────────│
│  Estudar React │ 10/02/25  │ Foco      │ Concluído│ 🗑  │
│                │ 14:30-14:31│           │         │     │
│  ────────────────────────────────────────────────────────│
│  Pausa         │ 10/02/25  │ Pausa C.  │ Concluído│ 🗑  │
│                │ 14:31-14:32│           │         │     │
│  ────────────────────────────────────────────────────────│
│  Resolver BUG  │ 10/02/25  │ Foco      │ Interr. │ 🗑  │
│                │ 14:32-14:34│           │         │     │
│  ────────────────────────────────────────────────────────│
│                                                          │
│  [↓ Carregar mais]         [🗑 Limpar Histórico]        │
│                                                          │
└──────────────────────────────────────────────────────────┘

Funcionalidades:
  • Click no header para ordenar (asc/desc)
  • Scroll horizontal em telas pequenas
  • Ícone lixo para deletar tarefa individual
  • Confirmação antes de limpar tudo
```

---

## 🎨 Componentes e Hierarquia

```
App
  │
  ├─ TaskContextProvider
  │   │
  │   ├─ ShowMessage
  │   │
  │   └─ MainRouter
  │       │
  │       ├─ Route: / (Home)
  │       │   └─ MainTemplate
  │       │       ├─ Menu
  │       │       ├─ Container
  │       │       │   └─ CountDown
  │       │       ├─ Container
  │       │       │   └─ MainForm
  │       │       │       ├─ DefaultInput
  │       │       │       ├─ Tips
  │       │       │       ├─ Cycles
  │       │       │       ├─ DefaultButton (Play/Stop)
  │       │       │       └─ MainForm
  │       │       └─ Footer
  │       │
  │       ├─ Route: /history
  │       │   └─ MainTemplate
  │       │       ├─ Menu
  │       │       ├─ Container
  │       │       │   └─ Heading
  │       │       ├─ Container
  │       │       │   └─ History
  │       │       │       ├─ Tabela c/ sorting
  │       │       │       └─ DefaultButton (Delete)
  │       │       └─ Footer
  │       │
  │       ├─ Route: /settings
  │       │   └─ MainTemplate
  │       │       ├─ Menu
  │       │       ├─ Container
  │       │       │   └─ Heading
  │       │       ├─ Container
  │       │       │   └─ DefaultInput (workTime)
  │       │       ├─ Container
  │       │       │   └─ DefaultInput (shortBreakTime)
  │       │       ├─ Container
  │       │       │   └─ DefaultInput (longBreakTime)
  │       │       ├─ Container
  │       │       │   └─ DefaultButton (Save)
  │       │       └─ Footer
  │       │
  │       ├─ Route: /about-pomodoro
  │       │   └─ MainTemplate
  │       │       ├─ Menu
  │       │       ├─ Container
  │       │       │   └─ AboutPomodoro
  │       │       └─ Footer
  │       │
  │       └─ Route: * (NotFound)
  │           └─ MainTemplate
  │               ├─ Menu
  │               ├─ Container
  │               │   └─ NotFound
  │               └─ Footer
  │
  └─ ScrollToTop (No UI, apenas scroll logic)
```

---

## 🔌 Integração de APIs Externas

```
┌─────────────────┐
│   react-toastify│  showMessage adapter
├─────────────────┤
│ toast.success() │─┐
│ toast.error()   │ ├─→ UI Notifications
│ toast.warn()    │ │
│ toast.info()    │─┘
└─────────────────┘

┌─────────────────┐
│   react-router  │
├─────────────────┤
│ BrowserRouter   │─┐
│ Routes          │ ├─→ SPA Navigation
│ Route           │ │
│ useLocation()   │─┘
└─────────────────┘

┌─────────────────┐
│   lucide-react  │
├─────────────────┤
│ PlayCircleIcon  │─┐
│ StopCircleIcon  │ ├─→ SVG Icons
│ SaveIcon        │ │
│ TrashIcon       │─┘
└─────────────────┘

┌─────────────────┐
│    date-fns     │  (pode ser usado)
├─────────────────┤
│ format()        │─→ Date Formatting
│ parse()         │
└─────────────────┘

┌─────────────────┐
│   localStorage  │  Browser API
├─────────────────┤
│ getItem()       │─┐
│ setItem()       │ ├─→ Data Persistence
│ removeItem()    │ │
│ clear()         │─┘
└─────────────────┘

┌─────────────────┐
│  Web Worker API │  Browser API
├─────────────────┤
│ new Worker()    │─┐
│ postMessage()   │ ├─→ Background Tasks
│ onmessage()     │ │
│ terminate()     │─┘
└─────────────────┘
```

---

## ⚙️ Fluxo de Configuração

```
Settings Page
      │
      ▼
┌─────────────────────────────────┐
│ <form onSubmit={handleSave}>    │
│  ├─ workTime Input              │
│  ├─ shortBreakTime Input        │
│  ├─ longBreakTime Input         │
│  └─ Submit Button               │
└──────────┬──────────────────────┘
           │
           ▼ (handleSave)
    ┌──────────────────────┐
    │ Validar inputs       │
    │ Criar payload config │
    └────────┬─────────────┘
             │
             ▼
    ┌──────────────────────┐
    │ dispatch(UPDATE_     │
    │ CONFIG, {payload})   │ (não implementado ainda)
    └────────┬─────────────┘
             │
             ▼
    ┌──────────────────────┐
    │ taskReducer          │
    │ state.config = {...} │
    └────────┬─────────────┘
             │
             ▼
    ┌──────────────────────┐
    │ localStorage update  │
    │ localStorage.        │
    │ setItem("state", ..□)│
    └────────┬─────────────┘
             │
             ▼
    ┌──────────────────────┐
    │ showMessage.success()│
    │ "Configurações      │
    │  salvas com sucesso" │
    └──────────────────────┘

NOTA: Botão de salvar está pronto, mas
      lógica de UPDATE_CONFIG ainda não
      foi implementada no reducer.
```

---

## 📊 Fluxo de localStorage

```
┌─────────────────┐
│   App inicia    │
└────────┬────────┘
         │
         ▼
┌──────────────────────────────┐
│ TaskContextProvider monta    │
│                              │
│ const storedState = JSON.parse│
│   (localStorage.getItem(      │
│    "state"))                 │
└──────────┬───────────────────┘
           │
      ┌────┴────────────────┐
      │                     │
   [EXISTE]             [NÃO EXISTE]
      │                     │
      ▼                     ▼
    Parse              initialTaskState
      │                     │
      └────────┬────────────┘
               │
               ▼
         ┌──────────────────┐
         │ State carregado  │
         │ (activeTask=null)│
         └────────┬─────────┘
                  │
                  ▼
         ┌──────────────────┐
         │ Qualquer mudança │
         │      no state    │
         └────────┬─────────┘
                  │
                  ▼
         ┌──────────────────┐
         │ useEffect([state])
         │  localStorage    │
         │ .setItem(...)    │
         └──────────────────┘

Dados persistidos:
  ✅ state.tasks[].{ id, name, duration, startDate, completeDate, interruptDate, type }
  ✅ state.config.{ workTime, shortBreakTime, longBreakTime }
  ✅ state.currentCycle

Dados NÃO persistidos:
  ❌ state.activeTask (reseta ao reload)
  ❌ state.secondsRemaining (recalculado)
  ❌ state.formattedSecondsRemaining (recalculado)
```

---

## 🎬 Ciclo Completo: Criar → Executar → Completar

```
1. CRIAÇÃO
   └─ User digita "Estudar TypeScript"
   └─ Clica em "▶ Iniciar"
   └─ handleCreateNewTask() é chamado
   └─ Valida input
   └─ Cria TaskModel:
      {
        id: "1707599400000",
        name: "Estudar TypeScript",
        duration: 1,                    (próximo ciclo é work)
        startDate: 1707599400000,
        completeDate: null,
        interruptDate: null,
        type: "workTime"
      }
   └─ dispatch({ type: START_TASK, payload: newTask })

2. REDUCER PROCESSA
   └─ taskReducer recebe START_TASK
   └─ nextCycle = 1 (getNextCycle(0))
   └─ secondsRemaining = 1 * 60 = 60
   └─ Retorna novo estado:
      {
        activeTask: newTask,           (ativado!)
        currentCycle: 1,               (incrementado)
        secondsRemaining: 60,
        formattedSecondsRemaining: "01:00",
        tasks: [newTask]               (adicionado)
      }

3. PERSISTÊNCIA
   └─ useEffect([state]) é acionado
   └─ localStorage.setItem("state", JSON.stringify(state))
   └─ Estado salvo no disco

4. WORKER INICIADO
   └─ useEffect([state]) também faz:
   └─ worker.postMessage(state)
   └─ Worker.js calcula endDate:
      endDate = 1707599400000 + (60 * 1000) = 1707599460000
   └─ Começa tick a cada 1 segundo

5. COUNTDOWN EM AÇÃO
   a) Worker calcula:
      secondsLeft = Math.floor((1707599460000 - 1707599401000) / 1000) = 59
   b) worker.postMessage(59)
   c) TaskContextProvider.onmessage recebe 59
   d) dispatch({ type: COUNT_DOWN, payload: { secondsRemaining: 59 } })
   e) UI atualiza para "00:59"
   f) Próximo tick em 1 segundo...
   [REPETE POR 60 SEGUNDOS]

6. COMPLETAR TAREFA
   a) Worker calcula:
      secondsLeft = Math.floor((endDate - Date.now()) / 1000) = 0 (ou negativo)
   b) Condição: if (secondsLeft <= 0)
   c) playBeepRef.current() toca o som
   d) dispatch({ type: COMPLETE_TASK })
   e) Reducer atualiza:
      {
        activeTask: null,              (limpo)
        secondsRemaining: 0,
        formattedSecondsRemaining: "00:00",
        tasks: [{...newTask, completeDate: Date.now()}]  (marcado!)
      }
   f) worker.terminate() para o Worker
   g) localStorage atualizado
   h) UI volta ao estado inicial

7. PRÓXIMA TAREFA
   └─ User vê UI limpo novamente
   └─ currentCycle ainda é 1
   └─ próximo getNextCycle(1) = 2
   └─ próximo tipo será "shortBreakTime"
   └─ User pode criar nova tarefa (pausa)
   └─ REPETE TODO O CICLO
```

---

## 🚨 Estados de Erro Possíveis

```
┌─────────────────────────────────────────┐
│        ERRO: Input Vazio                │
├─────────────────────────────────────────┤
│ if (!taskName) {                        │
│   showMessage.warn(                     │
│     "Please enter a valid task name."   │
│   )                                     │
│ }                                       │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│     ERRO: Task Interrompida             │
├─────────────────────────────────────────┤
│ onClick={() => {                        │
│   dispatch(INTERRUPT_TASK)              │
│   showMessage.error("Interrupt task.")  │
│ }}                                      │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│     ERRO: localStorage Indisponível     │
├─────────────────────────────────────────┤
│ Possível em:                            │
│ • Modo privado/anônimo                  │
│ • localStorage desabilitado             │
│ • Cota de armazenamento cheia           │
│                                         │
│ Resultado: Estado não persiste          │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│    ERRO: Worker não Suportado           │
├─────────────────────────────────────────┤
│ Possível em:                            │
│ • Navegadores antigos                   │
│ • Contexto seguro ausente                │
│                                         │
│ Resultado: Timer não funciona           │
└─────────────────────────────────────────┘
```

---

## 📈 Fluxo de Dados com Redux DevTools (Conceitual)

```
TIME: 00:00:00
ACTION: @@INIT
STATE:
├─ tasks: []
├─ activeTask: null
├─ currentCycle: 0
└─ secondsRemaining: 0

TIME: 00:00:15
ACTION: START_TASK
PAYLOAD:
├─ id: "1234567890"
├─ name: "Learn React"
└─ duration: 25
STATE:
├─ tasks: [TaskModel]
├─ activeTask: TaskModel
├─ currentCycle: 1 ⬆️
└─ secondsRemaining: 1500 ⬆️

TIME: 00:01:00
ACTION: COUNT_DOWN (every second)
PAYLOAD: { secondsRemaining: 1499 }
STATE:
├─ secondsRemaining: 1499 ⬇️
└─ formattedSecondsRemaining: "24:59"

TIME: 00:25:00
ACTION: COMPLETE_TASK
STATE:
├─ activeTask: null ⬆️
├─ secondsRemaining: 0
└─ tasks[0].completeDate: 1234567000 ⬆️
```

---

## 🎨 Esquema de Cores

```
Estilos Globais (styles/theme.css):
──────────────────────────────────

Cor Primária:    #007BFF (azul)
Cor Secundária:  #6C757D (cinza)
Cor Sucesso:     #28A745 (verde) ─── DefaultButton color="green"
Cor Perigo:      #DC3545 (vermelho) ─ DefaultButton color="red"
Cor Aviso:       #FFC107 (amarelo)
Cor Info:        #17A2B8 (ciano)

Ciclo Cores:
──────────

workTime:        #007BFF (azul) ────── 🔵
shortBreakTime:  #28A745 (verde) ───── 🟢
longBreakTime:   #6F42C1 (roxo) ────── 🟣
```

---

## 📐 Responsividade

```
Mobile First Approach
──────────────────

┌──────────────────┐
│   xs: 320px      │  Smartphone
│   sm: 576px      │  
└──────────────────┘

┌──────────────────┐
│   md: 768px      │  Tablet
│   lg: 992px      │
└──────────────────┘

┌──────────────────┐
│   xl: 1200px     │  Desktop
│   xxl: 1400px    │
└──────────────────┘

CSS Modules:
────────────
Cada componente tem styles.module.css
Evita conflito global de classes
Escopo local por arquivo
```

---

**Fim dos Diagramas e Guia Visual**
